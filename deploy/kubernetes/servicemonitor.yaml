apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: untold-api
  namespace: untold
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: untold-api
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
